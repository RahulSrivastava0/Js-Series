"use strict";   // treat all js code as newer version

// alert("Warning")     it's give popup on browser (but not used in node js)

// use should also focus on code redability

let name="Rahul"
let age=22
let isLoggedIn=true
let price=undefined
let cash=null

console.table([name,age,isLoggedIn,price,cash])

console.log(typeof null)
console.log(typeof isLoggedIn)